import { Isbn13DirectiveDirective } from './isbn13-directive.directive';

describe('Isbn13DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new Isbn13DirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
