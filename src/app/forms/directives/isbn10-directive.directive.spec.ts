import { Isbn10DirectiveDirective } from './isbn10-directive.directive';

describe('Isbn10DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new Isbn10DirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
